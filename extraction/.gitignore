# Local environment
.venv/
.env

# Python cache
__pycache__/
*.py[cod]

# Secrets
secrets/
credentials.json
token.json

# Runtime/browser state
runtime/
.wttj-playwright-profile/

# Generated data — ignore cache/temp, but track the two master job files
# so the GitHub Actions enrichment workflow can commit results back.
data/cache/
data/jobs/enrichment_live.jsonl
data/jobs/master_jobs_errors.jsonl
artifacts/
wttj_apply_debug/
wttj_email_debug/

# Root-level legacy log name
enrichment.logs
